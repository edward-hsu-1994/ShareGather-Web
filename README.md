# ShareGather Web

The official ShareGather landing page, built with React and Vite.

## Development

```sh
npm install
npm run dev
```

## Deployment

After pushing to `main`, GitHub Actions automatically builds and deploys the site to GitHub Pages. Before the first deployment, go to the repository's **Settings → Pages** and set **Source** to **GitHub Actions**.
