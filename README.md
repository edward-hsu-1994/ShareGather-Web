# ShareGather Web

ShareGather 的官方 Landing Page，以 React + Vite 製作。

## 開發

```sh
npm install
npm run dev
```

## 部署

推送至 `main` 後，GitHub Actions 會自動建置並部署至 GitHub Pages。首次部署前，請到 repository 的 **Settings → Pages**，將 Source 選為 **GitHub Actions**。
